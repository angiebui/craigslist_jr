<div class="container">
  <h1>Welcome to Craiglist Jr.</h1>
  <% @categories.each do |category| %>
    <ul>
      <%="<li><a href='/categories/" + category.id.to_s + "'>" + category.title + '</a></li>'%>
    </ul>
  <% end %>
</div>
